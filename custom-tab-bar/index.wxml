<van-tabbar active="{{ active }}" bind:change="onChange" active-color="#FFD000" borderStyle="black">
    <van-tabbar-item wx:for="{{ list }}" icon="{{ item.icon }}" wx:key="index" >
        {{item.text}}
    </van-tabbar-item>
</van-tabbar>

